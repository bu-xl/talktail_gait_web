/**
 * 화면에 띄우는 버전 문자열.
 *
 * git 버전 관리가 목적이 아니다 — **재배포가 실제로 반영됐는지**를 화면만 보고
 * 가리기 위한 것이다. 화면 변화가 없는 수정이 늘면서 육안으로 구분할 수단이 사라졌다.
 *
 * ★ 재배포 전에 이 값을 올린다. 안 올리면 목적 자체가 무너진다.
 *   앱은 `front_app/src/version.ts` 에 같은 규칙으로 따로 있다.
 */
export const APP_VERSION = "ver 1.0.0";
